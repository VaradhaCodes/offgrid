# Pilot QA — IDR Logger v1.0.0 on SM-S918B (Android 16, One UI 8.5) — Fri 4 Sep 2026 23:46 and 23:48

Verdict: the logger app works. Both sessions are complete, clean and usable. Two sessions of ~100 s each (15 s calib + ~70 s ride + 15 s stop-calib).

## Measured device capabilities (now facts, not assumptions)
| Stream | Sensor | Achieved | Gaps > 50 ms | Notes |
|---|---|---|---|---|
| acc, acc_unc | LSM6DSO (STMicro), minDelay 2404 µs | 420 Hz, median dt 2.38 ms, p99 2.38 ms | 0 | range ±157 m/s², max seen 27 m/s², no clipping |
| gyr, gyr_unc | LSM6DSO | 420 Hz | 0 | range ±34.9 rad/s, max seen 0.42 rad/s; calib bias z ≈ +0.004 rad/s (0.23°/s) — must be removed per session |
| mag, mag_unc | AK09918C | 100 Hz | 0 | field magnitude swings 3–71 µT (Earth ≈ 47 µT here): frame/disc/wheel disturb it. Magnetometer heading is unusable on this mount; do not use it. |
| grav, linacc, rotvec, gamerot | virtual | 210 Hz | 0 | |
| baro | LPS22HH | 25 Hz | 0 | |
| gnss_fix | GPS_PROVIDER | 1.00 Hz, max dt 1.04 s | 0 | acc_h median 3.8 m, max 5.8 m; speed/bearing present; not mock |
| gnss_status | | ~70 rows/s | | mean C/N0 of used signals 33 dB-Hz; GPS+GLO+GAL+BDS+QZSS+NavIC seen; L5 present (2331 rows) |
| gnss_raw | | ~63 rows/s | | pseudorange rate present; ADR state = 16 only (never VALID) → carrier phase unusable, as predicted |
| sys | | every 5 s | | battery 62 %, 33.5 °C, thermal 0, 5.9 GB free |
Data volume ≈ 13 MB per minute (~22 MB per 100 s session). 16 runs × 3.5 min ≈ 0.75 GB. Fine.
Clocks: events, IMU and GNSS all on elapsedRealtimeNanos; clock.csv maps to wall time. Anchors and distances are computed by the app and match my recomputation to < 0.5 m.

## Mount geometry from the calibration windows
Gravity in phone frame: run2 (−0.23, 9.01, 4.03), run3 (−0.43, 9.12, 3.75) m/s². The phone's long axis is ~24° from vertical (lying along the down tube), screen normal tilted up/out. Roll differs ~1° between the two runs: the tape mount is repeatable enough. |g| = 9.87 both runs.

## Ride geometry
Both rides were NOT on the proposed corridor A→J→B. They were on the hostel service road between Dining Hall 2 / Hostel 3B and Hostel 1C / Chess Garden (OSM ways 423056216 + 547288377), ~200 m long with one bend near the Volleyball/Basketball courts, ~150 m south of the blue road. run2 went west→east (user calls this A→B), run3 east→west. Both files are labelled BA: the direction toggle was not switched for run2. Anchor distance 158 m, GNSS path 196 m, ride 70 s, average 2.8 m/s, max 3.3 m/s (12 km/h). GNSS points sit on the OSM road: map matching will work here.

## Issues to fix before the main collection
1. Toggle direction AB/BA before each run (or fix in notes; I can relabel offline).
2. Decide the corridor and freeze it (see plan §1). Options: (a) proposed A→J→B, 812 m, straight + 90° turn; (b) extend the hostel road: continue from run2's end southwards along the Hostel 1C → Hostel 2A road, giving an L of ~450 m with a 90° bend; (c) keep the 200 m hostel road as is (works for the 50 m / 1 min benchmark example, weakest video).
3. Charge the phone before the main session (62 %).
4. Ride at the pace you will use in the video; the pilots were slow (12 km/h).
